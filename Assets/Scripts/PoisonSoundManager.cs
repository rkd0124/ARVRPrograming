using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PoisonSoundManager : MonoBehaviour
{
    public static PoisonSoundManager instance; //어디서든 접근할 수 잇도록..

    [Header("Audio Clips")]
    public AudioClip gunFireSound; //오디오 클립 삽입

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
